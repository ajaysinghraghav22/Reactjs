import React from 'react'
const UserContext = React.createContext()//creating context and storing in a variable when we create a context than it is mandatory to made a provider to provide the data.  and redux is used to pass the data from one place to another place in organized as we know context api only associated with only react


export default UserContext;